package com.axibase.tsd.driver.jdbc.enums;

public enum LexerTokens {
	AND, 
	AS, 
	ASC, 
	BETWEEN, 
	BY, 
	DESC,
	FROM, 
	GROUP, 
	HAVING, 
	IN, 
	INNER, 
//	INTERPOLATE,
	JOIN, 
	LIKE, 
	LIMIT, 
	NOT, 
	OR, 
	ORDER, 
	OUTER, 
	PERIOD, 
	REGEX, 
	ROW_NUMBER,
	SELECT, 
	USING,
	VALUE, 
	WHERE, 
	WITH, 
}